export const CALENDAR_URL = "https://calendar.app.google/st8KsrJpXH3VfWnH7";

// Secondary CTA target — the live sample tender page.
export const SAMPLE_URL = "/sample";

// Public URL of the aiestimator read-only product demo (Part B).
// Empty until the demo is deployed; /sample falls back to the static report below while empty.
export const DEMO_URL = ""; // SET WHEN DEMO IS LIVE

// A real example report tendr produced (a rail-bridge footing-works scope report).
// Shown on /sample as the concrete example until the live interactive demo (DEMO_URL) ships.
export const SAMPLE_REPORT_URL = "/sample-tender-report.pdf";
